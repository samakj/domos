#define HOSTNAME "${devices.sandbox.hostname}"
#define IP_ADDRESS "${devices.sandbox.ip}"

#define PURLEY_PARK_SSID "${wifi.networks.purley_park.ssid}"
#define PURLEY_PARK_PASSWORD "${wifi.networks.purley_park.password}"

#define THE_VALE_SSID "${wifi.networks.the_vale.ssid}"
#define THE_VALE_PASSWORD "${wifi.networks.the_vale.password}"

#define NTP_SERVER "${ntp.servers.uk}"

#define OTA_PASSWORD "${devices.sandbox.ota_password}"

#define DHT_PIN 13